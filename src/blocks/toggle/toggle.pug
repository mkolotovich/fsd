mixin toggle(label, status)
  if status == ''
    label.toggle
      input(type="checkbox")
      span(class="toggle__custom-checkbox")
      =label
  if status !== ''
    label.toggle
      input(type="checkbox" checked)
      span(class="toggle__custom-checkbox")
      =label